# --- keiba: local environment example ---
#
# Rename to `.env` if you want, then:
#   docker compose --env-file env.example up -d db
#
# NOTE:
# - `config/config.yaml` and `py64_analysis/alembic.ini` already default to:
#     postgresql+psycopg://postgres:postgres@localhost:5432/keiba
# - If you change any of the values below, update `config/config.yaml` accordingly.

# Docker Postgres
KEIBA_PG_USER=postgres
KEIBA_PG_PASSWORD=postgres
KEIBA_PG_DB=keiba
KEIBA_PG_PORT=5432

# Optional: make path resolution deterministic (any working dir)
# KEIBA_PROJECT_ROOT=C:\Users\yyosh\keiba
# KEIBA_CONFIG_PATH=C:\Users\yyosh\keiba\config\config.yaml

# JRA-VAN Data Lab (JV-Link)
# NOTE:
# - JVInit の引数は Sid（ソフトウェアID）です（利用キーとは別）
# - 利用キー（17桁英数字）は JVSetServiceKey で設定され、JVOpen/JVRTOpen の認証に使われます
# - ここでは「利用キー」を 4-4-4-4-1 の形式で置けるように KEIBA_JV_USE_KEY を用意します（内部でハイフン除去）
# - 秘匿情報なので `.env` に書いて git にコミットしないでください（.gitignoreで無視されています）
# KEIBA_JV_SID=SA000000
# KEIBA_JV_USE_KEY=XXXX-XXXX-XXXX-XXXX-X


